package com.nakpom.config

import org.springframework.context.annotation.Configuration

@Configuration
class DatabaseConfig {
    // Database configuration is primarily handled in application.yml
    // This class is reserved for custom database configurations if needed
}
